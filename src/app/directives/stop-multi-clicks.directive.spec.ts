import { StopMultiClicksDirective } from './stop-multi-clicks.directive';

describe('StopMultiClicksDirective', () => {
  it('should create an instance', () => {
    //const directive = new StopMultiClicksDirective();
    //expect(directive).toBeTruthy();
  });
});
