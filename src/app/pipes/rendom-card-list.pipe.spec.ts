import { RendomCardListPipe } from './rendom-card-list.pipe';

describe('RendomCardListPipe', () => {
  it('create an instance', () => {
    const pipe = new RendomCardListPipe();
    expect(pipe).toBeTruthy();
  });
});
